{
  "name": "electron-app",
  "version": "1.0.0",
  "main": "dist/main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder build --config electron-builder.json --publish never"
  },
  "devDependencies": {
    "electron": "^8.0.1",
    "electron-builder": "^22.3.2",
    "typescript": "^3.8.2"
  },
  "dependencies": {
    "electron-is-dev": "^1.1.0"
  }
}
